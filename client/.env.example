# Base URL for the API.
# Leave empty in development to use the Vite dev proxy (/api -> http://localhost:4000).
# Set to the deployed API origin in production, e.g. https://api.example.com
VITE_API_BASE_URL=""
